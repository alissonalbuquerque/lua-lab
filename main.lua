
local WString = require('utils.wrappers.String')
local WBoolean = require('utils.wrappers.Boolean')
local WNumber = require('utils.wrappers.Number')

local n1 = WNumber.new()
local n2 = WNumber.clone(n1)